/**
 * Arquivo de exemplo de configuração.
 * Copie este arquivo para config.js e preencha com suas credenciais reais do Supabase.
 * config.js está no .gitignore e NÃO deve ser commitado.
 *
 * Onde encontrar as credenciais:
 *   Supabase Dashboard → seu projeto → Project Settings → API
 */
window.SUPABASE_CONFIG = {
    url: 'SUA_URL_DO_SUPABASE_AQUI',
    anonKey: 'SUA_ANON_KEY_AQUI'
};
